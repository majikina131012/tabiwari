<table>
  <thead>
    <tr>
      <th><h2>グループ一覧</h2></th>
    </tr>
  </thead>
  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= link_to group.name, group_path(group), class: 'btn btn-light btn-lg m-2' %></td>
      </tr>
    <% end %>
  </tbody>
</table> 